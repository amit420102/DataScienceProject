https://geo.nyu.edu/catalog/nyu_2451_34572 -  this will be used to pull the details of the New York boroughs. I will be extracting data for Manhattan from the given data set and using it to sample the restaurants.
GET https://api.foursquare.com/v2/venues/search - this API will be used to fetch details of all the venues  for Manhattan and then will filter the restaurants only for below API.
GET https://api.foursquare.com/v2/venues/timeseries - this API wll be used to fetch the details of each of the given venue id (restaurants filtered above) for a given time range. This will be run in loop to get data for wach month for last 12 months.
Data fetched above will then be mapped on the NYC map to showcase the color coded rating with latest rating. Red rating means the preformance has gone down over time, Green means performance has gone up over time and orange means performance has been constant over time.
